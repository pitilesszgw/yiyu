{
  "name": "yiyu",
  "private": true,
  "scripts": {
    "build": "python3 -m pip install -r requirements.txt && python3 build_dist.py",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
